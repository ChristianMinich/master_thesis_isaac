# Config for synthetic_data_generation/vla/generate_vla_data.py
# Keys correspond 1:1 to the script's CLI flags (dashes -> underscores).
# CLI flags override these values.
backend: mock            # mock | isaac
num_envs: 4
num_episodes: 10
max_steps: 300
policy: scripted_goal    # random | zero | scripted_goal
noise_std: 0.05
seed: 42
output_dir: data/synthetic/vla_go2